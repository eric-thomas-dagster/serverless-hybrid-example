locations:
  - location_name: serverless-location
    code_source:
      package_name: serverless_location.definitions
    build:
      directory: .
    # Note: Serverless deployments don't use agent queues
    # agent_queue: serverless-queue
